{
  "name": "textkit",
  "version": "1.0.0",
  "description": "Deterministic text utility API. Slugify, HTML-to-text, text stats, case conversion.",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",
    "test": "node --test tests/api.test.js"
  },
  "devDependencies": {
    "yaml": "^2.9.0"
  }
}
